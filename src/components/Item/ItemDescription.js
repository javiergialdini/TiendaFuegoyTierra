export const ItemDescription = ( {description} ) => {
    return (
    <ul>
        <li>
            <p style={{paddingRight:'20px', fontSize:'20px'}}>{description}</p>
        </li>
    </ul>
    )
}